module.exports = class Patient {
  constructor(name, cpf, phoneNumber, email, gender, birthDate) {
    this.name = name,
    this.cpf = cpf,
    this.phoneNumber = phoneNumber,
    this.email = email,
    this.gender = gender,
    this.birthDate = birthDate
  }
}